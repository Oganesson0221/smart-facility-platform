# Lab images

Place the images you want to inspect in this folder, then set `IMAGE_NAME` in
`smart_facility_local_ai_lab.ipynb` to the selected filename.

Supported examples: `.jpg`, `.jpeg`, `.png`, and `.webp`. The notebook reads
only the image you select and writes annotated results to `lab_outputs/`.
