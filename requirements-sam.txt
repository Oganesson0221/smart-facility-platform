# Optional SAM 2 extras for the fire-exit segmentation path.
# Install these alongside the existing GPU requirements, then install the
# official SAM 2 package from the Meta repository as documented in README.md.
#
# The official SAM 2 docs currently require Python >=3.10, torch >=2.5.1,
# and torchvision >=0.20.1. If your environment is older than that, upgrade
# torch/torchvision before installing the official SAM 2 package.

-r requirements-nvidia.txt
torchvision>=0.20.1
hydra-core>=1.3.2
omegaconf>=2.3.0
iopath>=0.1.10
